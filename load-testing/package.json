{
  "name": "load-testing",
  "version": "1.0.0",
  "description": "https://www.artillery.io/docs/reference/engines/http",
  "scripts": {
    "test:load": "artillery run -o ${REPORT_DIR}/report-${ENV}.json --environment ${ENV} commonwealth-api.yml",
    "test:report": "artillery report --output ${REPORT_DIR}/report-${ENV}.html ${REPORT_DIR}/report-${ENV}.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "artillery": "^2.0.0-34",
    "artillery-plugin-ensure": "^1.1.3",
    "artillery-plugin-expect": "^2.3.1",
    "artillery-plugin-metrics-by-endpoint": "^1.2.0",
    "artillery-plugin-publish-metrics": "^2.4.0"
  }
}
