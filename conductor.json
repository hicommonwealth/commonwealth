{
  "scripts": {
    "setup": "pnpm install; if [ -f \"$CONDUCTOR_ROOT_PATH/.env\" ]; then ln -sf \"$CONDUCTOR_ROOT_PATH/.env\" .env; elif [ ! -f .env ]; then cp .env.example .env; fi; docker-compose up -d; pnpm migrate-db",
    "run": "pnpm start-all"
  },
  "runScriptMode": "nonconcurrent"
}
