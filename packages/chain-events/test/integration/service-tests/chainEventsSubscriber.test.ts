describe("Tests for the ChainSubscriber service", () => {
  it("Should create listeners for every selected chain");
  it("Should consume chain-events from a chain");
  it("Should push processed chain-events to the appropriate RabbitMQ queue");
});
