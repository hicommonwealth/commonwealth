describe("Tests for the App service", () => {
  it("Should be able to fetch entities");
  it("Should be able to fetch event activity");
  it("Should support posting ChainSubscriber status data");
  it("Should be able to fetch ChainSubscriber status data");
});
