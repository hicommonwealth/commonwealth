describe('emitChainEventNotifs', () => {
  it('should not emit any notifications if there are no events', () => {});
  it('should not throw if emitting a notification fails', () => {});
  it('should fail to create notifications for unsupported events without throwing', () => {});
  it('should emit a notification for each given proposal event', () => {});
});
