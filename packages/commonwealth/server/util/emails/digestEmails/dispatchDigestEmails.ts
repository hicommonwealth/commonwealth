export async function dispatchDigestEmails() {
  // attempt to acquire daily lock
  // attempt to acquire weekly lock
  // if lock is acquired batch fetch users
  // send emails to users in batches
}
