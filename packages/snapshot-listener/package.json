{
  "name": "snapshot-listener",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc -b && tsc-alias",
    "clean": "rm -rf build",
    "check-types": "tsc --noEmit",
    "lint-all": "eslint './**/*.ts'",
    "start": "ts-node-dev --max-old-space-size=4096 --respawn src/index.ts",
    "test": "NODE_ENV=test ts-mocha './test/**/*.spec.ts'"
  },
  "engines": {
    "node": "20.x"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.1",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "node-fetch": "2",
    "tslog": "^3.3.4"
  },
  "devDependencies": {
    "tsx": "^4.7.2"
  }
}
