{
  "name": "@hicommonwealth/scripts",
  "private": "true",
  "version": "0.1.0",
  "description": "Scripts",
  "files": [
    "build"
  ],
  "main": "build/index",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf build",
    "check-types": "tsc --noEmit",
    "test": "NODE_ENV=test ts-mocha '__tests__/**/*.spec.ts'",
    "generate-stakeholder-groups": "npx ts-node ./generate-stakeholder-groups"
  },
  "dependencies": {
    "sequelize": "^6.32.1"
  },
  "devDependencies": {}
}
