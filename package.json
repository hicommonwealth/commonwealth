{
  "private": true,
  "scripts": {
    "heroku-postbuild": "tsc --project packages/commonwealth/server/tsconfig.json",
    "start": "yarn --cwd packages/commonwealth start",
    "build": "yarn --cwd packages/commonwealth build"
  },
  "engines": {
    "node": "14.x"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hicommonwealth/commonwealth.git"
  },
  "keywords": [],
  "workspaces": [
    "packages/commonwealth"
  ],
  "dependencies": {
  },
  "devDependencies": {
  }
}
