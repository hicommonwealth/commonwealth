{
  "name": "@hicommonwealth/sitemaps",
  "private": "true",
  "version": "0.1.0",
  "description": "Sitemaps generator and related code",
  "files": [
    "build"
  ],
  "main": "build/index",
  "types": "src/index",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf build",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "aws-sdk": "^2.814.0"
  }
}
