import { expect } from 'chai';

describe('Community stake', () => {
  it('should set and get community stake', () => {
    expect(true).to.be.false;
  });

  it('should fail set when community not found', () => {
    expect(true).to.be.false;
  });

  it('should fail set when community stake has been configured', () => {
    expect(true).to.be.false;
  });

  it('should fail set when community namespace not configured', () => {
    expect(true).to.be.false;
  });

  it('should get empty result when community stake not configured', () => {
    expect(true).to.be.false;
  });
});
