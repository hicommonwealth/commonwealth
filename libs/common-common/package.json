{
  "name": "@hicommonwealth/common-common",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": "18.x"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hicommonwealth/commonwealth.git"
  },
  "keywords": [],
  "files": [
    "build"
  ],
  "main": "build/index",
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf build",
    "check-types": "tsc --noEmit",
    "build-contracts": "npx hardhat compile",
    "lint": "../../scripts/lint-new-work.sh",
    "lint-all": "eslint './**/*.ts'",
    "publish-rmq-msg": "ts-node --project tsconfig.json -r tsconfig-paths/register src/rabbitmq/scripts/publishRmqMessageScript.ts"
  },
  "dependencies": {
    "@hicommonwealth/core": "*",
    "@hicommonwealth/adapters": "*",
    "async-rwlock": "^1.1.1",
    "rascal": "^14.4.5",
    "redis": "4.2.0"
  },
  "devDependencies": {
    "@types/expect": "^24.3.0",
    "@types/mocha": "^10.0.0",
    "mocha": "^6.2.2",
    "ts-generator": "^0.1.1",
    "ts-mocha": "^10.0.0"
  }
}
