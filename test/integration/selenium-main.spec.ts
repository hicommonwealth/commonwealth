/* eslint-disable */
describe('Tests the commonwealth app with Selenium', () => {
  beforeEach(() => {

  });
  afterEach(() => {

  })

  it('POM test', () => {
    let baseUrl = 'https://www.google.com/';
  })
})
